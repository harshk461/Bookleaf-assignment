import type { TicketCategory, TicketPriority } from '@bookleaf/shared';

export const DEFAULT_TICKET_CATEGORY: TicketCategory = 'general_inquiry';
export const DEFAULT_TICKET_PRIORITY: TicketPriority = 'medium';
